# Релиз: подставьте ваш домен
# Скопируйте в .env.production перед npm run build

VITE_API_URL=https://your-domain.com/api
# Если Mini App открывается по https://your-domain.com/app — раскомментируйте:
# VITE_BASE_PATH=/app/
